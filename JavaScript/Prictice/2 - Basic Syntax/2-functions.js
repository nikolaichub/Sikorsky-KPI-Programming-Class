'use strict';

// Функции

let a = 2;
let b = 6;

const avarage = (a, b) => (a + b) / 2;
const square = a => a*a;
const cube = a => a*a*a;

console.log(avarage(a,b));
console.log(square(a));
console.log(cube(a));
